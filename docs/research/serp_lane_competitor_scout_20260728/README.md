# SERP lane: competitor scout + journey levers — routed snapshot 2026-07-28

Owner-routed snapshot of the SERP capture lane's competitor-scout work
unit: typed competitor ledger (spec + promotion ladder + channels),
Understanding-cycle installation (scout pass + ordering rule; two-phase
shape with the Reddit lane), journey levers J1-J5, the Tower 28 live
trial (SERP scout + 14 native threads + price surfaces), the Deliver
retention/offense note, and the cross-company dogfood (CeraVe /
AeroPress).

**Authority and currentness:** the LIVING surface remains the operator
staging folder `C:\tmp\forseti-serp-lane\` (owner staging decision
2026-07-27) until the lane stabilizes; this folder is the durable
routed record as of 2026-07-28. If the two diverge, staging is ahead.
Expected next supersession: the full-bank megadogfood analysis pass
(~2026-07-30, n≈100 subjects) re-judges lane cells F4-F21 and the
emitter's candidate ladder.

**Evidence and instruments** (raw capture data stays outside Git, on
the operator drive): `C:\tmp\forseti-serp-megadogfood-20260727\`
(query bank, run ledger, extractions, emitter + fixture test in
`bin\`), `C:\tmp\forseti-tower28-scout-20260727\` (scout trial packets,
PDP captures), `C:\tmp\forseti-tower28-reddit-20260728\` (14 native
thread packets + composition read).

Reading order: `serp_lane_v0.md` (entry point / findings ledger) ->
`competitor_ledger_spec_v0.md` (types, ladder, cycle installation,
J1-J5) -> `deliver_note_repair_room_retention_v0.md` ->
`tower28_scout_trial_findings_v0.md` + `tower28_ci_onepager_demo_v0.md`
(trial evidence + demo deliverable) ->
`dogfood2_cross_company_note_v0.md` (generalization check).

Standing non-claims carried throughout: counts of observed cards only,
never prevalence/volume/share; US-parameterized is not physically
US-local; blocks are stop signals; raw data outside Git.

Execution is commissioned via two handoffs, in order:
`docs/prompts/handoffs/serp_lane_phase1_scout_execution_handoff_v0.md`
(seeds -> rolling harvest -> merged vs+J5 queue -> priced ledger +
trigger-thread queue), then
`docs/prompts/handoffs/serp_lane_phase2_native_return_execution_handoff_v0.md`
(native return: Reddit-lane consumption, Channel 3, J3 settlement, J5
delta, evidence-targeted return probes).
