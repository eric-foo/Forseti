# Phase A Customer-Pull Frontier Evidence-First Ordering Delegated Adversarial Code Review-And-Patch Prompt v0

```yaml
retrieval_header_version: 1
artifact_role: Forseti delegated adversarial code review-and-patch prompt
scope: Cross-vendor adversarial review and bounded patch of the evidence-first customer-pull frontier ordering at commit 7b148f14a0ea6d97e0a14de971009fb536aebc02.
use_when:
  - Couriering the evidence-first Phase A frontier-ordering change for its commissioned post-implementation review.
  - Attacking whether support posture, independent origins, engagement, and reported behavior produce a commercially useful and truth-preserving investigation order.
authority_boundary: retrieval_only
branch_or_commit: codex/phase-a-hydration-pack-cap-pilot @ 7b148f14a0ea6d97e0a14de971009fb536aebc02
open_next:
  - AGENTS.md
  - .agents/workflow-overlay/README.md
  - .agents/workflow-overlay/review-lanes.md
  - .agents/workflow-overlay/delegated-review-patch.md
  - forseti/product/spines/judgment/claim_support/forseti_intelligence_claim_support_contract_v0.md
  - forseti/product/spines/judgment/claim_support/forseti_semantic_evidence_integration_contract_v0.md
  - docs/workflows/phase_a_customer_evidence_completion_path_v0.md
stale_if:
  - The exact reviewed revision is not available.
  - A target file differs from the bytes at 7b148f14a0ea6d97e0a14de971009fb536aebc02 before the receiver begins its own patch.
  - The owning claim-support, semantic-integration, or delegated-review authority changes before review.
```

## Commission

Run one de-correlated delegated adversarial code review-and-patch pass on the
exact implementation commit below. Review the actual repository diff and the
complete named targets, not this prompt's summary. The business goal is to lead
commercial investigation with customer points carrying stronger bounded
support, while using reported purchase behavior only to break otherwise equal
ties. Done means the ordering is deterministic, commercially intelligible, and
cannot promote a weaker point by laundering source roles, origins, engagement,
or generic ownership into stronger support.

This is preparation-only until the operator binds an eligible controller. The
author/home family is OpenAI. The controller must be a different upstream
vendor/model lineage with direct repository access. This is a who-constraint,
not a runtime-model recommendation. Same-vendor, unknown-lineage, no-repo,
self, and Codex-managed controller substitutes are invalid. The receiving actor
is the controller and must not launch a replacement controller. If eligibility,
exact revision, clean state, write isolation, or repository access cannot be
verified, return the corresponding blocker without substituting another route.

```yaml
output_mode: review-report
template_kind: none
edit_permission: patch-only
review_routing_status: routed
delivery: operator_courier_only
access: repo
delegate_eligibility: different_vendor_lineage_with_direct_repo_access
author_home_model_family: OpenAI
controller_model_family: operator_to_fill_different_vendor_required
current_receiving_actor_role: controller
target_kind: delegated_code_review_and_patch
review_method: workflow-code-review
required_revision: 7b148f14a0ea6d97e0a14de971009fb536aebc02
revision_mode: exact
reviewed_diff: eaf0fea102649c8c4d517e610eb3785060577392..7b148f14a0ea6d97e0a14de971009fb536aebc02
branch: codex/phase-a-hydration-pack-cap-pilot
report_destination: docs/review-outputs/adversarial-artifact-reviews/phase_a_customer_pull_frontier_evidence_first_ordering_delegated_review_v0.md
patch_destination: controller-owned review branch/worktree created from the exact required revision
receiver_binding:
  receiver_class: receiver_to_bind
  binding_state: receiver_to_bind
  launch_checkout: receiver_to_observe
  effective_target_worktree: C:\Users\vmon7\.codex\worktrees\e4f8\orca
  required_revision: 7b148f14a0ea6d97e0a14de971009fb536aebc02
  revision_mode: exact
  capability_proof: receiver_to_observe
  no_concurrent_writer_state: receiver_to_observe
```

Delegate-facing constants:

- `environment_baseline`: Windows host, PowerShell-first: use PowerShell syntax for shell/test commands; use absolute paths resolvable from any cwd; invoke `python`, never `python3`; do not pass Windows drive-letter paths or heredocs through bash.
- `lifecycle_hard_stop`: A delegate or receiver does not commit, push, open or update a PR, merge, stash, reset, clean the worktree, or run repository-hygiene actions unless the commission explicitly grants that action.
- `decorrelation_commission`: `delivery: operator_courier_only` · `access: repo` · `delegate_eligibility: different_vendor_lineage_with_direct_repo_access`; same-vendor, unknown-lineage, no-repo, self, and Codex-managed controller substitutes are invalid; a manager-prefixed target path is neutral; if no eligible controller is available the prompt remains unexecuted.

Before source review, verify the receiver binding, exact clean `HEAD`, direct
write capability, and no concurrent writer. Record the concrete controller
family and exact reviewed revision. Then read `AGENTS.md`, the overlay README,
the code-diff target-kind and de-correlation sections of
`.agents/workflow-overlay/delegated-review-patch.md`, the Review Doctrine and
Rules sections of `.agents/workflow-overlay/review-lanes.md`, and the three
fitness sources named below. Do not bulk-read historical review outputs or
prompt history.

## Exact Patch Scope

Patch only these four targets, plus write the commissioned report at the report
destination. Everything else is read-only and flag-only.

- `[selection-runtime]` `forseti-harness/judgment/phase_a_evidence_selection.py`
- `[selection-tests]` `forseti-harness/tests/unit/test_phase_a_evidence_selection.py`
- `[semantic-contract]` `forseti/product/spines/judgment/claim_support/forseti_semantic_evidence_integration_contract_v0.md`
- `[phase-a-workflow]` `docs/workflows/phase_a_customer_evidence_completion_path_v0.md`

The reviewed commit also contains mechanical provenance/escaping corrections
to two earlier review records. They are outside this commission's patch scope;
flag a material interaction but do not edit those records. Do not modify packet
v3, evidence membership, relation truth, production outputs, captured corpora,
or historical receipts. Do not rerun extraction or semantic reconciliation. Do
not add a commercial-pull score, behavior subtype classifier, packet v4, new
provider workload, or latency optimization. On a design-level problem that
cannot close inside the four targets, return `NEEDS_ARCHITECTURE_PASS`, revert
any partial target edits, and return findings only.

## Fitness Reference

Use these as the controlling fitness sources:

- `forseti/product/spines/judgment/claim_support/forseti_intelligence_claim_support_contract_v0.md` for independent recurrence, cross-venue corroboration, source-role separation, engagement-as-resonance, and causal/prevalence ceilings;
- `forseti/product/spines/judgment/claim_support/forseti_semantic_evidence_integration_contract_v0.md` v55 for the exact frontier ordering contract; and
- `docs/workflows/phase_a_customer_evidence_completion_path_v0.md`, section `Active commercial point-entry boundary`, for the operator-visible business question and workflow.

The required behavior is:

1. keep retailer-supported points in the retailer-first queue and community-only customer points in the community-discovery queue;
2. within each queue, order first by bounded support posture, then more independent supporting origins, then more materially engaged supporting evidence;
3. treat reported behavior as a separate commercial-strength tie-breaker only after those evidence-strength dimensions are equal;
4. keep creator-authored evidence outside customer truth and keep engagement venue/role/metric-relative;
5. preserve all 105 proposition dispositions, literal evidence membership, packet v3, the thirteen-origin point-pack cap, and downstream relation/quote behavior; and
6. remain deterministic and no-provider.

The intended change is ordering only. It does not claim that all behaviors are
equally valuable, that two venues always beat one, that engagement proves
prevalence, or that the frontier is a Deliver recommendation.

## Required Adversarial Attacks

Find across the full target before patching. Every finding, citation, and diff
hunk must carry its target label.

1. `[selection-runtime]` Prove or refute that the implemented posture tuple exactly matches the v55 prose. Attack every boundary: two origins/two roles, two origins/one role, one materially engaged origin, and one quiet origin.
2. `[selection-runtime]` Construct the dangerous near-miss that motivated the change: a weaker generic trial/ownership behavior versus a more strongly supported non-behavior point. The stronger point must lead. Then make every strength dimension equal; the reported-behavior point must win only that tie.
3. `[selection-runtime]` Attack false cross-role strength. Check whether `customer_support_roles` and the stored independence keys can count one echoed or derived origin as both independent and cross-role. Confirm creator-authored material cannot supply the second role or any customer support.
4. `[selection-runtime]` Attack the coarse posture boundary: two weak cross-role origins can currently lead many strong same-role origins because posture is ordered before count. Determine whether this is faithful to the claim-support contract or a universal source-ranking false green. Patch only if the current fitness sources decide it; otherwise report the unresolved design tradeoff.
5. `[selection-runtime]` Attack engagement counting. The key counts materially engaged evidence IDs, not engagement magnitude. Verify that it cannot compare Reddit points with retailer helpful votes, reward duplicate rows from one origin as independent corroboration, or imply that several low signals exceed one very high within-venue signal.
6. `[selection-runtime]` Attack queue separation and accounting. Retailer-first must not become retailer-only; community discovery must not be described as retailer corroboration; every considered proposition must occur exactly once; subject-filtered and nonpromoted propositions must remain visible.
7. `[selection-runtime]` Attack protected safety/costly points. Admission must remain unchanged, and the ordering change must not silently make a protected high-loss point disappear. Distinguish visibility from top-of-queue priority rather than inventing a new priority score.
8. `[selection-runtime]` Attack determinism and malformed input: missing/wrong-typed origin counts, duplicate roles, duplicate material evidence IDs, missing proposition IDs, reordered packet rows, and a local frontier rehash. Find any accepted drift or unstable tie.
9. `[selection-tests]` Check whether the new test merely re-implements the tuple and misses a full-frontier failure. Add the smallest ground-truth falsifier needed for any real defect you find; do not broaden into an unrelated suite rewrite.
10. `[semantic-contract]` `[phase-a-workflow]` Compare every changed sentence with runtime behavior. Specifically attack “cross-role independent recurrence,” “material source-native resonance,” “inside one posture,” and “reported behavior remains a tie-breaker.” Do not allow prose to imply a cross-platform magnitude comparison, commercial-pull score, prevalence, or Deliver authority.
11. All targets: check that this is the smallest complete ordering change in the existing v3 consumer and does not create a second evidence authority or recurring provider/review step.

The highest-loss plausible false green is a queue that looks evidence-first but
actually promotes a weak point because roles, origins, or engagement rows were
counted in a way the evidence contract does not support. The second is a
generic behavior point still outranking stronger evidence because behavior was
moved only superficially rather than made a true final tie-breaker.

## Evidence To Confirm, Not Trust

- The author reports exact clean implementation commit `7b148f14a0ea6d97e0a14de971009fb536aebc02` with parent `eaf0fea102649c8c4d517e610eb3785060577392`.
- The targeted falsifier failed before the change because `weaker-behavior` led `stronger`, then passed after the change with `stronger` first and an otherwise equal behavior point winning its tie.
- Focused selection/consumer/semantic tests passed.
- Full `python -m pytest -q forseti-harness --tb=no -p no:warnings` passed with only existing skips.
- Full `pwsh .github/scripts/run-doc-gates.ps1` passed 24/24 after two mechanical pre-existing review-record corrections in the same commit.
- `git diff --check` and Python compilation passed.
- The authoritative local packet used for dogfood was `C:\tmp\forseti-summer-fridays-polarity-repair-replay-20260817-v0\incremental-terminal-migration-v10\phase_a_evidence_packet_v3.json`.
- The author reports deterministic frontier SHA-256 `fafbfe794b9540ba4e35ef0be84e81042de684a1b79b1042e880ac281852b13f`, unchanged accounting of 105 considered propositions as 8 retailer-first, 93 community-discovery, and 4 nonpromoted, and a top-13 community slice changing from 13 reported-behavior points / 41 supporting origins / 28 materially engaged supporting rows to 4 / 92 / 51.

Recompute what the repository and available packet can establish. Do not trust
the counts, hash, prose, or test claims because they appear here. If the local
packet is unavailable, report dogfood reproduction as `not_run` rather than
manufacturing a substitute or changing corpora.

## Validation And Return

After any patch, run at minimum:

```powershell
$env:PYTHONPATH='forseti-harness'
python -m pytest -q forseti-harness/tests/unit/test_phase_a_evidence_selection.py forseti-harness/tests/unit/test_phase_a_evidence_consumer.py forseti-harness/tests/unit/test_semantic_evidence_integration.py
python -m pytest -q forseti-harness --tb=no -p no:warnings
pwsh .github/scripts/run-doc-gates.ps1
git diff --check
```

Report every command as passed, failed, blocked, or not run with the observed
result. A baseline failure never excuses a candidate failure. Do not call the
result accepted, ready, or mergeable.

Return findings first under the `workflow-code-review` lane: include severity
(`critical`, `major`, or `minor`) and confidence, exact citations, the failure
mechanism, `minimum_closure_condition`, and `next_authorized_action`. Include a
`considered_and_defended` section for attacks that held. Then return the
uncommitted bounded diff, neutral decision-sufficient citations for every
change, validation evidence, one overall verdict, per-target sub-verdicts when
materially different, and residual risks. Record `reviewed_by`, `authored_by`,
the concrete controller/author vendor families, required revision, and reviewed
revision in the durable report. The controller creates the findings and
citations; do not split review from execution.

The home/Chief Architect must adjudicate the findings, diff, verdict, and
residuals as claims before any returned line is kept. It may accept, modify, or
reject every hunk and must revert rejected changes. It then closes every
self-closable material finding in the same adjudication turn, reruns the
class-level falsifiers and byte/scope checks, and batches any commit/push/PR/
merge work into one later land step. The delegated return is decision input,
not approval, validation, mandatory remediation, or permission for lifecycle
actions.
