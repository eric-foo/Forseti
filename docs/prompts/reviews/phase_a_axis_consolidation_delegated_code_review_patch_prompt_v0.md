---
retrieval_header_version: 1
artifact_role: Delegated code review-and-patch prompt
scope: Phase A hydration-axis origin-normalized evidence consolidation at implementation checkpoint c7f6d6cb
use_when:
  - Couriering the frozen axis-consolidation implementation to an eligible different-vendor controller with direct repository access.
authority_boundary: retrieval_only
---

# Phase A axis consolidation — delegated code review and patch

This prompt is preparation-only until an eligible external receiver proves the
binding below. Review the frozen implementation adversarially, patch only
confirmed material defects inside the named set, run the focused validation,
and return a decision-ready report. Done means one cold consumer can use the
ten completed hydration points as one readable axis without losing or changing
any point, claim-relative direction, condition, comparator, quote, credited
origin, source binding, engagement boundary, thread concentration, or access to
the 8,360 point-candidate dispositions.

```yaml
preflight_defaults: docs/prompts/templates/shared/forseti_preflight_defaults_v0.md v0 - constants bound; deltas stated inline.
delivery: operator_courier_only
access: repo
target_kind: delegated_code_review_and_patch
delegate_eligibility: different_vendor_lineage_with_direct_repo_access
author_vendor: OpenAI
delegate_vendor: operator_to_fill
output_mode: paste-ready-chat
output_destination: courier conversation
edit_permission: patch-only
required_revision: c7f6d6cb5bbd8bbf1ea8a0963c778f0801b5ea65
revision_mode: ancestor
implementation_parent: 7961035ab4ec979ecb2ca4fcf1df305191fb5f66
branch: codex/phase-a-hydration-pack-cap-pilot
review_routing_status: routed
receiver_binding:
  receiver_class: receiver_to_bind
  launch_checkout: receiver_to_observe
  effective_target_worktree: C:\Users\vmon7\.codex\worktrees\e4f8\orca
  required_revision: c7f6d6cb5bbd8bbf1ea8a0963c778f0801b5ea65
  revision_mode: ancestor
  clean_at_bind: required
  direct_write_capability: receiver_to_verify
  no_concurrent_writer: receiver_to_verify
dirty_state_allowance: clean at bind; afterward only delegate-authored edits inside the named patch set
historical_and_external_outputs: read-only
commit_push_pr_merge_stash_reset_clean: forbidden
```

Before source review:

1. Record the delegate vendor and stop as ineligible if it is OpenAI, unknown,
   undisclosed, lacks direct repository access, or cannot patch the bound root.
2. Read `AGENTS.md`, `.agents/workflow-overlay/README.md`, the code-diff target
   section of `.agents/workflow-overlay/delegated-review-patch.md`, and the code
   review lane in `.agents/workflow-overlay/review-lanes.md`.
3. Verify the worktree is clean and the required revision is an ancestor of
   `HEAD`; record that clean `HEAD` as immutable `reviewed_revision`. If the
   branch advances after capture, use a separate clean worktree at that
   revision.
4. Prove direct write capability without a synthetic mutation, confirm no
   concurrent writer, then inspect `7961035ab4ec979ecb2ca4fcf1df305191fb5f66...c7f6d6cb5bbd8bbf1ea8a0963c778f0801b5ea65`
   before reconstructing broader history.

Delegate-facing environment baseline: Windows host, PowerShell-first; use
PowerShell syntax and absolute paths where cwd is ambiguous; invoke `python`,
never `python3`; do not pass Windows drive-letter paths or heredocs through
bash.

Lifecycle hard stop: do not commit, push, open or update a PR, merge, stash,
reset, clean the worktree, or run repository-hygiene actions.

De-correlation commission: operator courier only, direct repository access,
and a different upstream vendor/model lineage from the OpenAI author are
mandatory. Same-vendor, unknown-lineage, no-repo, self, and Codex-managed
controller substitutes are invalid. A manager-prefixed target path is neutral.
If no eligible controller is available, leave this prompt unexecuted.

## Authority and named patch set

Load the actual target diff, then read these owners as needed:

- `forseti/product/spines/judgment/claim_support/forseti_intelligence_claim_support_contract_v0.md`
- `docs/workflows/phase_a_customer_evidence_completion_path_v0.md`
- the implementation and tests in the named set below.

Patch authority is limited to exactly:

1. `forseti-harness/judgment/phase_a_evidence_axis_consolidation.py`
2. `forseti-harness/runners/run_phase_a_evidence_axis_consolidation.py`
3. `forseti-harness/tests/unit/test_phase_a_evidence_axis_consolidation.py`
4. `docs/workflows/phase_a_customer_evidence_completion_path_v0.md`

Everything else—including this prompt, completed point artifacts, packet and
bundle sources, and all `C:\tmp` dogfood outputs—is read-only and flag-only. Do
not add packet v4, a new evidence authority, a new selector, normalized
engagement scoring, extraction, semantic relabeling, provider calls, a generic
axis framework, prevalence claims, or a Deliver recommendation. A required
out-of-set or design-level change returns `NEEDS_ARCHITECTURE_PASS` with no
partial design patch.

## Why independent review is required

The home structural tests and deterministic reprojection may share the same
mistaken normalization assumption. The blind quality judge was isolated but
same-vendor, and only the hydration axis has been exercised. The first compact
shape also demonstrated the danger: one provider repetition changed two
point-relation origin lists until exact direct per-point lists were added. A
cross-vendor reviewer should therefore attack semantic preservation and cold
consumer usability, not merely hashes or formatting.

## Required attacks

At minimum attack:

- exact parity: every one of the ten bounded points, 146 placements, 32 unique
  origins, 37 evidence items, 56 quote records, 181 companion meanings, and
  8,360 point-candidate dispositions remains accounted and cold-resolvable;
- claim-relative direction: the direct support/counter/adjacent origin arrays
  exactly equal the origin union derived from placements for that point, cannot
  override them, and cannot hide duplicate rows or a relation mutation;
- semantic separation: positive hydration, hydration shortfall, direct drying,
  delayed drying/cracking, Balm Dotcom, and Rhode remain distinct; conditions,
  product versions, comparison subjects, polarity, uncertainty, protected lanes,
  and retained-unmerged state cannot move between them;
- origin integrity: repeated rows or threads from one actor receive one origin,
  distinct actors in one thread remain distinct with concentration visible, and
  the same origin cannot silently change its independence key;
- quote and companion integrity: exact and unavailable quote records remain
  typed and bound; companion meanings remain placement-relative, store one
  stable semantic meaning per ref, and cannot donate a condition or axis;
- engagement: Reddit posts, Reddit comments, Amazon reviews, Sephora reviews,
  and other supported venue/role surfaces cannot be misclassified or compared
  across surface, metric, venue, or calendar year; source-native resonance must
  never increase independent-origin credit;
- cold-source and hash integrity: stale axis, point, selection, quote, packet,
  bundle, candidate-inventory, or stored-manifest lineage fails before a view is
  accepted, including coherently rehashed saved-view mutations;
- compactness without laundering: shared facts are stored once, but no required
  fact is available only by an undocumented sibling-path guess; full candidate
  arrays are dereferenced rather than duplicated;
- runner honesty: unsupported schema, incomplete axis, malformed JSON, stale
  source, overwrite, or partial failure cannot leave a fake success output;
- test validity: falsifiers fail for the intended boundary and are not made
  green by fixture omissions or Summer Fridays-specific strings;
- workflow claims: rederive the receipt values and preserve its nonclaims—one
  hydration axis, same-vendor judge, descriptive latency, no selector change,
  no prevalence, and no packet v4.

Real read-only dogfood:

- source axis:
  `C:\tmp\forseti-phase-a-hydration-axis-pack-20260822-v0\hydration_axis_pack_v2.json`
  (raw SHA-256
  `42f7fa0ca3c7d2000c77e97d37a429aa32c04c320aa2fa000a68b114acf9c77d`);
- consolidated view:
  `C:\tmp\forseti-phase-a-axis-consolidation-20260822-v0\consolidated_view_v5.json`
  (raw SHA-256
  `40321faeecdc25de493a2fc6c9d534e0410add91f5ee48b6c706f6cdcdbf90aa`);
- experiment receipt:
  `C:\tmp\forseti-phase-a-axis-consolidation-20260822-v0\experiment_receipt_v1.json`
  (raw SHA-256
  `8f97a45de735dff2d62438e90f75cc6b1910878c91741b3d98f5b151ab4b75de`).

Re-derive material facts from those sources. If any path is unavailable, mark
dependent checks not run; do not infer them from workflow prose and do not
modify or overwrite the artifacts.

## Focused validation

Run each command separately after any patch and report its actual result. Do
not list or run the whole `forseti-harness` suite locally; required CI owns that
broad duplicate gate. A failure, timeout, block, or not-run result remains
visible.

```powershell
python -m pytest -q forseti-harness/tests/unit/test_phase_a_evidence_axis_consolidation.py forseti-harness/tests/unit/test_phase_a_evidence_selection.py forseti-harness/tests/unit/test_phase_a_evidence_consumer.py
python -m py_compile forseti-harness/judgment/phase_a_evidence_axis_consolidation.py forseti-harness/runners/run_phase_a_evidence_axis_consolidation.py
python forseti-harness/runners/run_phase_a_evidence_axis_consolidation.py validate --view C:\tmp\forseti-phase-a-axis-consolidation-20260822-v0\consolidated_view_v5.json
python .agents/hooks/check_retrieval_header.py --changed --strict
python .agents/hooks/check_placement.py --strict --base origin/main
python .agents/hooks/check_prompt_output_mode.py --strict --base origin/main
python .agents/hooks/check_review_routing.py --strict --base origin/main
python .agents/hooks/check_map_links.py --strict --base origin/main
git diff --check
```

For every accepted defect, add or strengthen one focused regression that fails
on the frozen implementation for the intended reason and passes after the
patch. Do not use a baseline error, cache accounting, or token saving to waive
any semantic defect.

## Return

Return, in order:

1. receiver binding, author/delegate vendors, required revision, and captured
   `reviewed_revision`;
2. severity-ordered findings with exact file/line evidence, impact, confidence,
   and minimum closure condition;
3. the exact bounded diff, or an explicit no-patch statement;
4. neutral authority/source citations for every material finding;
5. each validation and dogfood result, including not-run items;
6. verdict `clean_for_architect_adjudication`,
   `patched_for_architect_adjudication`, `NEEDS_ARCHITECTURE_PASS`, or
   `blocked`; and
7. residual risk, including the non-independent delegate-authored lines and the
   untested non-hydration-axis boundary.

Your return is decision input only. The home Chief Architect must adjudicate
every finding, diff, verdict, and residual before any returned change is kept.
